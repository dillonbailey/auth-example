import React from 'react';

import logo from './logo.svg';

const Main = () => (
  <div>
    <h1>Logged In</h1>
    <p>Based on email validation and verification code, we can now allow you a single-use session for submitting the form.</p>
    <p>The utilised resources for this e.g. sending the form, would all be further protected and require authorizations.</p>
    <p>Closing your browser should sign you out.</p>
  </div>
);

export default Main;

